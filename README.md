# Sabot - fertige Builds

Hier liegt nur das gebaute Programm, damit Sabot sich selbst
aktualisieren kann: die App liest `version.json` und laedt bei
Bedarf `sabot.apk`.

Der Quellcode liegt woanders. Dieser Zweig wird bei jedem Build
ueberschrieben.
